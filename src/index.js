const fetch = require('node-fetch')

class Cobinhood {
  constructor({ options }) {
    this.options = options
    this.prices = this.prices
  }
  prices(paris, callback) {
    return callback(
      null,
      paris 
    )
  }
}

module.exports = Cobinhood
